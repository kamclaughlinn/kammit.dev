# Spotify Web Playback SDK (local copy)

Chrome MV3 blocks remote scripts, so this file must live in the extension.

Source: https://sdk.scdn.co/spotify-player.js

Re-download if needed:

```bash
curl -fsSL -A "Mozilla/5.0" -o spotify-player.js "https://sdk.scdn.co/spotify-player.js"
```
