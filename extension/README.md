# KAMmit Record Player (Chrome extension — optional)

The player now lives **on kammit.dev itself** (no install). This extension is kept as an optional / experimental packaging of the same idea.

Prefer the site floating vinyl at the bottom-right of kammit.
